# Data folder

Drop input datasets here. Contents are gitignored — do not commit data with PII.

Suggested structure:
- `raw/` — untouched source files
- `processed/` — cleaned datasets produced by the data-fetcher agent
