---
description: Profile a dataset — quick health check before analysis
argument-hint: <dataset path>
---

Profile the dataset at `$1` using the `data-profiling` skill.

Save the profile to `outputs/_profile_<filename>.md` and show the user the key flags (high-null columns, duplicates, unusual cardinality) inline.
